type Sprite
  x: byte
  y: byte
  tile: byte
  flags: word
  _pad_word: word
  _pad_byte: byte
end

section data vars at $1000
    one: Sprite = { x: 0, y: 0, tile: 0, flags: 0, _pad_word: 0, _pad_byte: 0 }
end

export func main()
  a := one.x
  ret
end
