type Point
  x: byte
  y: byte
end

type Scene
  sprites: Point[4]
end

const BadField = offset(Point, z)
const BadIndexUnknown = offset(Scene, sprites[Nope].x)
const BadIndexRange = offset(Scene, sprites[9].x)

export func main()
  ld a, 0
end
